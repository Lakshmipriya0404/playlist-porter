import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#181d38", //dark blue
      light: "#37406e",
    },
    secondary: {
      main: "#533996", //violet
    },
    otherColor: {
      main: "#575d7e", //grey
    },
  },
});
